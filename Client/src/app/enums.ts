export enum PlayPause {
    play = 'play',
    pause = 'pause'
}

export enum SessionEnum {
    Create = 'Create',
    End = 'End',
    Leave = 'Leave'
}

export enum ShowOrHide {
    Show = 'Show',
    Hide = 'Hide'
}

export enum LocalStorageEnum {
    SessionId = 'sessionId',
    User = 'user',
    Qr = 'qr'
}
